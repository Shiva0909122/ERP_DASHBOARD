{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for the ERP Dashboard",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",  // Starts the backend using nodemon
    "install:backend": "cd backend && npm install",  // Installs dependencies for backend
    "start:frontend": "cd frontend && npm start",  // Starts the frontend development server
    "install:frontend": "cd frontend && npm install",  // Installs dependencies for frontend
    "build:frontend": "cd frontend && npm run build",  // Builds the production-ready frontend
    "test": "echo \"Error: no test specified\" && exit 1"  // Placeholder for tests
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "debug": "^4.3.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.4",
    "qs": "^6.12.2",
    "semver": "^7.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
